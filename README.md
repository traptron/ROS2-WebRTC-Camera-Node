# ROS2-WebRTC-Camera-Node
Docker-образ для ROS2 Jazzy, который принимает изображение от ESP32-CAM (через micro-ROS) и транслирует его по WebRTC.

> 🔹 Поддерживает Python 3.12  
> 🔹 Совместим с Windows (Docker Desktop)  
> 🔹 Использует `aiortc` для WebRTC

---

## Содержание

- [Описание](#-описание)
- [Требования](#-требования)
- [Установка](#-установка)
- [Запуск](#-запуск)
- [Использование](#-использование)


---

## [Описание](#-описание)

Этот образ запускает ROS2-ноду, которая:

- Подписывается на топик `/camera/image_raw` (изображение от ESP32-CAM)
- Транслирует изображение через WebRTC
- Доступна через веб-браузер по адресу `http://localhost:8080`

---

## [Требования](#-требования)
- Docker Desktop (Windows, macOS) или Docker Engine (Linux)
- ESP32-CAM с запущенным micro-ROS агентом, публикующим изображение в топик `/camera/image_raw`

---

## [Установка](#-установка)

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/traptron/ROS2-WebRTC-Camera-Node.git
cd ros2-webrtc-camera
```

### 2. Соберите Docker-образ
```bash
docker build -t ros2-webrtc-node .
```
---
## [Запуск](#-запуск)

### 1. Запустите контейнер
```bash
docker run -it --rm -p 8080:8080 ros2-webrtc-node
```

### 2. Откройте в браузере
```
http://localhost:8080
```
---
## [Использование](#-использование)
Если ESP32-CAM работает на хосте, вы можете подключить контейнер к той же сети:
```bash
docker run -it --rm -p 8080:8080 --network=host ros2-webrtc-node
```
Настройка ROS_DOMAIN_ID
Если используете несколько ROS-доменов:
```bash
docker run -it --rm -p 8080:8080 -e ROS_DOMAIN_ID=1 ros2-webrtc-node
```
